import 'package:flutter/material.dart';

class ResponsesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Contenu de Responses',
        style: const TextStyle(fontSize: 18),
      ),
    );
  }
}
